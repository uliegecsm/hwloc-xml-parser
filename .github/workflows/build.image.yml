name: Build image

on:
    push:
        branches:
            - main
        paths:
            - 'requirements/*'
            - 'docker/dockerfile'
            - .github/workflows/build.image.yml
    pull_request:
        branches:
            - main
        paths:
            - 'requirements/*'
            - 'docker/dockerfile'
            - .github/workflows/build.image.yml

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:

    set-vars:
        uses: ./.github/workflows/set-vars.yml

    build-image:
        needs: [set-vars]
        runs-on: [ubuntu-latest]
        container:
            image: docker:latest
        permissions:
            contents: read
            packages: write
        strategy:
            fail-fast: false
            matrix:
                version: [
                    2.9.0,
                    2.12.0,
                    2.14.0
                ]
        steps:
            - uses: actions/checkout@v7

            - uses: docker/setup-qemu-action@v4

            - uses: docker/setup-buildx-action@v4

            - uses: docker/login-action@v4
              with:
                  registry: ${{ needs.set-vars.outputs.CI_REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - uses: docker/build-push-action@v7
              with:
                  context   : .
                  platforms : linux/amd64,linux/arm64
                  push      : ${{ github.ref == 'refs/heads/main' }}
                  file      : docker/dockerfile
                  tags      : ${{ needs.set-vars.outputs.CI_IMAGE }}:${{ matrix.version }}
                  cache-from: type=registry,ref=${{ needs.set-vars.outputs.CI_IMAGE }}:${{ matrix.version }}
                  cache-to  : type=inline
                  labels    : "org.opencontainers.image.source=${{ github.repositoryUrl }}"
                  build-args: |
                      HWLOC_VERSION=${{ matrix.version }}
